import 'package:flutter/material.dart';

class MiningSimulatorScreen extends StatelessWidget {
  const MiningSimulatorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Mining Simulator'),
      ),
      body: const Center(
        child: Text('Mining Simulator Coming Soon!'),
      ),
    );
  }
}